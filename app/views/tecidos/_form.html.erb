<%= form_for @tecido, :html => { :class => "form-horizontal tecido" } do |f| %>

  <% if @tecido.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@tecido.errors.count, "error") %> prohibited this tecido from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @tecido.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :nome, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :nome, :class => 'form-control' %>
    </div>
    <%=f.error_span(:nome) %>
  </div>
  <div class="form-group">
    <%= f.label :largura, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :largura, :class => 'form-control' %>
    </div>
    <%=f.error_span(:largura) %>
  </div>

  <div class="form-group">
    <div class="col-lg-offset-2 col-lg-10">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                tecidos_path, :class => 'btn btn-default' %>
    </div>
  </div>

<% end %>
